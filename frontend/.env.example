# ─── Apuntador del backend ────────────────────────────────────────────────
# Cambia SOLO estas dos líneas para mover la app entre local y producción.

# URL que usa la app cuando corre en el navegador (ionic serve).
API_URL=http://localhost:8200/api

# URL que usa la app cuando corre como app nativa (Android / iOS).
# Emulador de Android:  http://10.0.2.2:8200/api
# Dispositivo físico:   http://<IP-de-tu-PC>:8200/api
# Producción:           https://api.tu-dominio.com/api
API_URL_NATIVE=http://10.0.2.2:8200/api
